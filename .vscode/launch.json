{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Scoop",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            "args": [
                "-m",
                "scoop"
            ]
        }
    ]
}
